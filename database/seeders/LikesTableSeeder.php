<?php

// namespace Database\Seeders;

// use Illuminate\Database\Seeder;
// use Illuminate\Support\Facades\DB;
// use Carbon\Carbon;

// $likes = [];

// for ($i = 1; $i <= 150; $i++) {
//     $randomDate = Carbon::now()->subDays(rand(0, 365))->subHours(rand(0, 23))->subMinutes(rand(0, 59))->subSeconds(rand(0, 59));

//     $likes[] = [
//         'user_id'        => rand(1, 40), // случайный пользователь от 1 до 50
//         'institution_id' => rand(1, 18), // случайное учреждение от 1 до 30
//         'created_at'     => $randomDate,
//         'updated_at'     => $randomDate,
//     ];
// }

// return $likes;
